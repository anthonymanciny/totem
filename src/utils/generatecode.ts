// import { sendAuthenticationCode } from './mailer';
// import { IUsuario } from '../interface/usuario_interface';


// // Função para gerar um código de autenticação de 6 dígitos
// const generateAuthCode = (): string => {
//   return Math.floor(100000 + Math.random() * 900000).toString(); // Gera um código de 6 dígitos
// };

// // Exemplo de uso
// const userEmail = 'usuario@dominio.com'; // Substitua pelo e-mail do usuário
// const authCode = generateAuthCode();

// sendAuthenticationCode(userEmail, authCode);